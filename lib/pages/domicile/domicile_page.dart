import 'package:flutter/material.dart';

class DomicilePage extends StatelessWidget {
  const DomicilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Domicile will be issued from here"),
      ),
    );
  }
}
